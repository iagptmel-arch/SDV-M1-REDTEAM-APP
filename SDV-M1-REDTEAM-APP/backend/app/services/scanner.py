"""
Service de scan de ports
Détection des ports TCP et UDP ouverts
"""


def scan_ports(host: str, ports: list[int] | None = None) -> list[dict]:
    """Scanne les ports d'un hôte et retourne les ports ouverts."""
    # TODO: implémenter le scan de ports (nmap ou raw sockets)
    return []
